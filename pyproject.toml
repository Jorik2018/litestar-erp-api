[tool.poetry]
name = "litestar-erp-api"
version = "0.1.0"
description = ""
authors = ["Erik Antonio Alarcón Pinedo <C8VMM@ferrovial.com>"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.12"
litestar = "^2.22.0"
uvicorn = "^0.48.0"
gqlalchemy = "^1.8.0"
dotenv = "^0.9.9"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"

[tool.poetry.scripts]
start = "uvicorn app.main:app --reload"
